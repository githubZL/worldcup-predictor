# 世界杯预测模型 v0.8 实施计划

> **给自动化协作者的要求：** 按任务逐步实施；如需要使用 superpowers 工作流，优先使用 `superpowers:subagent-driven-development` 或 `superpowers:executing-plans`。清单使用 `- [ ]` 跟踪进度。

**目标：** 将预测引擎升级到 `poisson-v0.8`，新增结构化 `modelSignals` 因子层，同时保持已有 API 字段向后兼容。

**架构：** 第一版继续把实现放在 `server/src/services/predictionService.js`，贴合当前项目模式。新增因子摘要、体能、阵容可用性和冷门风险辅助函数，并通过 `predictionBreakdown.modelSignals` 输出。

**技术栈：** Node.js ESM、`node:test`、现有 Fastify/Prisma 数据流。

## 任务 1：新增 v0.8 预测信号测试

**文件：**

- 修改：`server/src/services/predictionService.test.js`
- 修改：`server/src/services/dataQuality.test.js`
- 修改：`server/src/services/modelReviewService.test.js`

- [ ] 编写失败测试：
  - 模型版本应为 `poisson-v0.8`
  - `predictionBreakdown.modelSignals` 存在
  - signals 包含 `baseStrength`、`form`、`production`、`fatigue`、`environment`、`tactical`、`availability`、`upsetRisk`

- [ ] 运行定向测试：

```bash
node --test server/src/services/predictionService.test.js
```

预期：失败，因为 v0.8 和 `modelSignals` 尚未实现。

## 任务 2：实现 v0.8 模型信号

**文件：**

- 修改：`server/src/services/predictionService.js`

- [ ] 新增辅助摘要函数：
  - confidence 标签
  - 近期状态日期摘要
  - 阵容可用性
  - 体能边际
  - 冷门风险

- [ ] 新增 `buildModelSignals()`，并在比分矩阵可用后从 `buildPrediction()` 调用。

- [ ] 保留旧输出字段：
  - 除 `MODEL_VERSION` 和 `risk` 外，现有字段保持不变。
  - `risk` 由 `modelSignals.upsetRisk.level` 映射。

- [ ] 运行定向测试：

```bash
node --test server/src/services/predictionService.test.js
```

预期：通过。

## 任务 3：项目验证与真实库回测

**文件：**

- 如果测试暴露问题再修改对应文件。

- [ ] 运行全部服务测试：

```bash
npm test
```

预期：0 个失败。

- [ ] 运行生产构建：

```bash
npm run build
```

预期：Vite 构建退出码为 0。

- [ ] 校验 Prisma schema：

```bash
npx prisma validate --schema prisma/schema.prisma
```

预期：schema 有效。

- [ ] 运行真实数据库回测：

```bash
env ENABLE_LIVE_WEATHER=false npm run report:model-backtest
```

预期：报告使用 `poisson-v0.8`，并返回当前已完赛比赛样本。

## 自检

- 旧 API 字段保持兼容。
- 每场预测都有完整 `modelSignals`。
- 体能、阵容、环境、战术和冷门风险均可解释。
- 不把赛后本场数据喂回赛前预测。
- 竞彩赔率仍不在本阶段接入。
