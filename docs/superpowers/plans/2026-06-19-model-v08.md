# 世界杯预测模型 v0.8 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Upgrade the prediction engine to `poisson-v0.8` with a structured `modelSignals` factor layer while preserving existing API fields.

**Architecture:** Keep the first implementation inside `server/src/services/predictionService.js` to follow the current project pattern. Add helper functions for factor summaries, fatigue, availability, and upset risk, then expose them under `predictionBreakdown.modelSignals`.

**Tech Stack:** Node.js ESM, `node:test`, existing Fastify/Prisma data flow.

---

### Task 1: Add v0.8 Prediction Signal Tests

**Files:**
- Modify: `server/src/services/predictionService.test.js`
- Modify: `server/src/services/dataQuality.test.js`
- Modify: `server/src/services/modelReviewService.test.js`

- [ ] **Step 1: Write failing tests**

Add assertions that model version is `poisson-v0.8`, `predictionBreakdown.modelSignals` exists, and signals include `baseStrength`, `form`, `production`, `fatigue`, `environment`, `tactical`, `availability`, and `upsetRisk`.

- [ ] **Step 2: Run targeted tests**

Run: `node --test server/src/services/predictionService.test.js`

Expected: FAIL because v0.8 and `modelSignals` are not implemented.

### Task 2: Implement v0.8 Model Signals

**Files:**
- Modify: `server/src/services/predictionService.js`

- [ ] **Step 1: Add helper summaries**

Add helper functions for confidence labels, recent form date summary, lineup availability, fatigue edge, and upset risk.

- [ ] **Step 2: Build `modelSignals`**

Add `buildModelSignals()` and call it from `buildPrediction()` after the score matrix is available.

- [ ] **Step 3: Preserve legacy output**

Keep existing fields unchanged except `MODEL_VERSION` and `risk`, which should map from `modelSignals.upsetRisk.level`.

- [ ] **Step 4: Run targeted tests**

Run: `node --test server/src/services/predictionService.test.js`

Expected: PASS.

### Task 3: Verify Project and Real Backtest

**Files:**
- No file edits unless tests expose an issue.

- [ ] **Step 1: Run all service tests**

Run: `npm test`

Expected: 0 failures.

- [ ] **Step 2: Run production build**

Run: `npm run build`

Expected: Vite build exits 0.

- [ ] **Step 3: Validate Prisma schema**

Run: `npx prisma validate --schema prisma/schema.prisma`

Expected: schema is valid.

- [ ] **Step 4: Run real database backtest**

Run: `env ENABLE_LIVE_WEATHER=false npm run report:model-backtest`

Expected: report uses `poisson-v0.8` and returns current finished-match sample.
